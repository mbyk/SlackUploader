<template lang="erb">
    <header id="header">
        <div class="container">
          <div class="title">
            <%= ActionController::Base.helpers.link_to('SlackUploader', '/') %>
          </div>
          <div class="header-navigation">
            <ul>
              <li v-if="!logged_in"><%= ActionController::Base.helpers.link_to 'Login', '/login' %></li>
              <li v-if="logged_in"><%= ActionController::Base.helpers.link_to 'Logout', '/logout', method: :delete, data: { confirm: 'ログアウトしてもよいですか？' } %></li>
            </ul>
          </div>
        </div>
      </header>
</template>

<script>
export default {
  props: {
    logged_in: Boolean,
  },
  data() {
    return {
    }
  }
}
</script>
